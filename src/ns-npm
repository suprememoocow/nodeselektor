
script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if ! nodeselektor_init .; then
  npm "$@"
  exit
fi

if ! ${script_dir}/ns-npm-validate; then
  rm -rf "${nodeselektor_root_path}/node_modules/"
fi

npm "$@"

${script_dir}/ns-save-module-version
